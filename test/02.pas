program tekashi;

var
	a, b : Integer;
	c, d, a, b : Real;
	e : array [1..10] of Real;

procedure bar;
begin
	a := (x + z)/e + 123.32 + (32 - 14) / 2;
	a := 4
end;

function foo(a, b, c : Integer) : Integer;
var
	x, y : Real;
	b : Integer;
begin
	c := d;
	d := 2
end;

begin
	x := readln(e);
	if color < red then
		c := 10
	else
		b := 2;

	i := 0;
	while i < 10 do
	begin
		x := writeln(i);
		i := i + 1
	end;

	r := 0
end.
