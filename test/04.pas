program tekashi;

var
	a, b : Integer;
	c, d : Real;
	e : array [1..10] of Real;
	x, i, r : Real;
	color, red : Integer;

procedure bar;
begin
	a := (b + d)/e + 123.32 + (32 - 14) / 2;
	a := 4
end;

function foo(a, b, c : Integer) : Integer;
begin
	c := d;
	d := 2
end;

begin
	i := 0;
	a := 1;
	i := 10;
	r := i + 0 + a
end.
