program tekashi;

var
	a, b : Integer;
	c, d : Real;
	e : array [1..10] of Real;
	x, i, r : Real;
	color, red : Integer;

procedure bar;
begin
	a := (b + d)/e + 123.32 + (32 - 14) / 2;
	a := 4
end;

function foo(a, b, c : Integer) : Integer;
begin
	c := d;
	d := 2
end;

begin
	if color < red then
		c := 10
	else
		b := 2;

	i := 0;
	while i < 10 do
	begin
		i := i + 1
	end;

	r := 0
end.
